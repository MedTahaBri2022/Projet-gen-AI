Open the file in Visual Studio Code (VS Code).

Run the app.py file.

Insert the following commands exactly:

cd uml_generator\uml_env

then

& .\\Scripts\activate.bat 

then

streamlit run app.py

(Verify that all modules are installed)

The solution will be generated.